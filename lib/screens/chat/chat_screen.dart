import 'package:flutter/material.dart';
class ChatScreen extends StatelessWidget{ const ChatScreen({super.key}); @override Widget build(BuildContext context){ return Scaffold(appBar: AppBar(title: const Text('Pesan ala TikTok')), body: ListView.builder(itemCount: 15, itemBuilder: (c,i)=> ListTile(leading: CircleAvatar(child: Text('${i+1}')), title: Text('User ${i+1}'), subtitle: const Text('Hai...'), trailing: const Text('2m'))));}}
