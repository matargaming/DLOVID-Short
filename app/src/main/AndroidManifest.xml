<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="DLOVID-Short"
        android:theme="@style/Theme.DLOVID"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">
        
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>

        <!-- MENU LOGIN - LOGO DLOVID GOLD -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:theme="@style/Theme.DLOVID">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MENU DRAMA - 13 KATEGORI -->
        <activity android:name=".DramaActivity" android:exported="false" android:theme="@style/Theme.DLOVID" />
        
        <!-- FULLSCREEN VERTIKAL PLAYER - TULISAN GOLD - FIX APPCOMPAT -->
        <activity 
            android:name=".PlayerActivity" 
            android:exported="false" 
            android:theme="@style/Theme.DLOVID.Fullscreen"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation" />

        <!-- BERANDA TIKTOK STYLE -->
        <activity android:name=".BerandaActivity" android:exported="false" android:theme="@style/Theme.DLOVID" />
        <activity android:name=".PlusActivity" android:exported="false" android:theme="@style/Theme.DLOVID" />
        <activity android:name=".PesanActivity" android:exported="false" android:theme="@style/Theme.DLOVID" />
        <activity android:name=".AkunActivity" android:exported="false" android:theme="@style/Theme.DLOVID" />

    </application>
</manifest>
